To highlight - we use CONDITIONAL FORMATTING
Like we want to highlight the cells above value 500
HOME - CONDITIONAL FORMATTING - HIGHLIGHT cell rules - Greater than

If you want to remove the conditional formatting
Conditional formatting - Manage rules

CONDITIONAL FORMATTING - also used for finding duplicates
To remove the duplicates HOME - Remove duplicates - unselect all - select Unique ID (customer ID)


# SUBTOTALS   (This is in ribbon)

TO FIND TOTAL FOR EACH GROUP
First filter the data / sort the data in an order
-- To find subtotal for each group
DATA - Subtotal

ANOTHER SUBTOTAL - NOT SUBTOTALS only SUBTOTAL

# SUBTOTAL  (it is a function)
It has refrence - the column
and it has function 1-11 (includes the hidden values)
101-111 9does not include the hidden values)

SUM VS SUBTOTAL (subtotal shows sum according to the filter)
If we choose only east region sales it wll automatically show only that value


DATA VALIDATION WITH EXCEL

Eg - If we have a table to enter 1,2,3... and want to ensure that only numerical data gets added we use data validation
SELECT the rows then only - DATA - DATA VALIDATION - LIST - select source
It will create a dropdown for each cell to select from (the cells that were selected)
We can use it for decimals, whole numbers, length of words

Now we want a customer error messgae if user inputs something else - 
SELECT the rows then only - DATA - DATA Validation - ERROR Alert Tab - 



PIVOT TABLE
Once we make a pivot table and now we have sum of sales
Now we want another column % difference of sales
Drag sales again in the value field and click on it - value field setting - show value as - now select the appropriate

PIVOT TABLE from multiple worksheets
Create pivot table for 1 sheet
go to that pivot table
go to data field where you see aloo the columnw - MORE TABLES

now we need add in POWER PIVOT
select OKay
Now go to options - add ins - in drop down select - COMM add IN - go - select microsoft power pivot for excel
click on table - add to data model, then click on another worksheet and add to data model
In power pivot table - Diagram view

One to many relation
PArent table (one) to child table (many)

Another way - go to data view - right click on the column - manage relationship
How to make KPI
under this power pivot - manage - autosum dropdown - select the option - then click on KPI
select absolute value - select colour - select icon - okay
KPI is created - close power pivot
expand the columns you will seee the KPI



### IF
Give absolute refrence inside if (like name cell L2 = goal)
=IF(F5>=goal,"Yes","No")

we want to calculate BONUS (if all 4 weeks is above 8000 and goal reached yes)
NESTED If
=IF(AND(H5= "Yes",MIN(B5:F5)>=8000) = TRUE,"BONUS","NO BONUS")


# COUNTIF
If we want to count number of yes - countif & countifs
=COUNTIF(H5:H9, "Yes")

# SUMIF
=SUMIF(B3:B272,G3,E3:E272)

# IFERROR
=IFERROR(VLOOKUP($B12,'Master Emp List'!$A$1:$I$38,3,FALSE),"EMP NOYT FOUND")



# VLOOKUP

1. select the column from your table for which you are looking for data
2. Select the table (from where you want other related data)
3. count the column
4. Optional True False

DRAWBACK - the 1. (select the column from your table for which you are looking for data) 
this column should be 1st column in lookup refrence table
It is slow

Same for HLOOKUP - it is horizontal of VLOOKUP


# INDEX and MATCH
Alternate to VLOOKUP
Faster

=INDEX('INDEX MATCH Master Emp List'!A1:I38,10,3)
1. Table refrence
2. row of that refrence table
3. column of that refrence table

MATCH - it will take some values (data you are lookinf for) and give back it's position
1. select your value you want output of from your table
2. In refrence table sleect the refrence column
3. 0,1,-1





# TEXT BASED OPERATIONS
LEFT, RIGHT, MID
LUXSKY - RIGHT(sleect the LUXSKY cell, 3) - It will return LUX

# LEN()
checks number of character in a cell


# SEARCH()
For first name
=LEFT(A2,SEARCH(" ",A2))
If whole name is A2 , we are searching left from space Patrick harry - so it will give left of Space

for last name
=RIGHT(A2,LEN(A2) - SEARCH(" ",A2))



# 365 functions

1 filter - 
1. select table
2. select column from that table
filter(table1, country_column=France)   


















































































